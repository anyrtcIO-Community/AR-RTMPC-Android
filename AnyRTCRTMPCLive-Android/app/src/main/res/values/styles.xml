<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--直播样式-->
    <style name="style_live_ibtn_controls">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_marginLeft">@dimen/audience_controls_margin</item>
        <item name="android:layout_marginTop">@dimen/audience_controls_margin</item>
        <item name="android:layout_marginBottom">@dimen/audience_controls_margin</item>

    </style>

</resources>
